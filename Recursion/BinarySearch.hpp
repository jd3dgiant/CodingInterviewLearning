#pragma once
#include <Vector>
/// Implement a function to perform a binary search on a sorted array of integers to find the index
/// of a given integer, Comment on the efficiency of this search, and compare it with other search
/// methods.

int BinaryIntSearch(const std::vector<int> sortedArray, const int valueToFind);
